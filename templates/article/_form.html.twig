{{ form_start(form) }}
<div class="containerI mt-4 mb-5">
	<div class="wrapper">
	  <div class="image">
		<img src="" id="img">
	  </div>
<div class="content">
		<div class="icon">
<i class="fas fa-cloud-upload-alt"></i></div>
<div class="text">
No file chosen, yet!</div>
</div>
<div id="cancel-btn">
<i class="fas fa-times"></i></div>
<div class="file-name">
File name here</div>
</div>
<input type="button" onclick="defaultBtnActive();" id="custom-btn" value="Choose a file"/>
<div hidden>{{form_widget(form.Picture)}}</div>
  </div>
    {{ form_widget(form) }}
    <button class="btn btn-outline-warning btn-lg btn-block">{{ button_label|default('Save !!') }}</button>
{{ form_end(form) }}
